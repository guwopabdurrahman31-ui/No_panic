# 공황장애 극복 PWA — 안티그래비티 인계 문서 (2026-07-30)

이 프로젝트는 Cowork(Claude)에서 여기까지 개발·테스트를 진행했고, 지금부터 안티그래비티에서 이어서 작업합니다. 아래는 새 환경이 알아야 할 전체 맥락입니다.

## 0. 프로젝트 한 줄 요약
공황장애가 있는 개발자 본인(Brian)이 실제 발작 시 사용하려고 만든, 근거기반(임상논문·가이드라인) 무료 오프라인 PWA. 웹과 구글 플레이스토어에 동일 코드로 배포 예정. 광고 없음, 완전 무료, 개인정보·증상 기록은 기기 밖으로 절대 나가지 않음(서버 전송 없음, 전부 localStorage).

## 1. 폴더 구조 (실제 배포 대상)
```
/Users/soo/AI_agents_20260516/panic/
├── panic-app/                          ← 실제 배포용 앱 (여기가 핵심)
│   ├── index.html                      (880줄, 전체 앱 단일 파일 — HTML+CSS+JS)
│   ├── manifest.json                   (PWA manifest)
│   ├── sw.js                           (서비스워커, 오프라인 캐싱)
│   └── assets/music/
│       ├── README.txt
│       ├── low-1.mp3, low-2.mp3        (낮고 깊은 울림, 총 9.2MB)
│       ├── nature-1.mp3, nature-2.mp3  (빗소리·파도소리, 총 5.9MB)
│       └── synth-1.mp3, synth-2.mp3    (잔잔한 전자음, 총 11.1MB)
├── 공황앱_음악근거_메모.md              ← 설계결정·근거 전체 기록 (프로젝트 성경, 꼭 참고)
├── HANDOFF_안티그래비티_인계.md         ← 이 문서
├── 공황_진정게임.html, 공황_진정앱.html  ← 초기 시제품(구버전, 무시해도 됨)
```

**중요: `panic-app/index.html` 하나가 사실상 전체 앱입니다.** HTML/CSS/JS가 한 파일에 다 들어있는 단일 파일 구조(single-file PWA)입니다. 음악 파일 6개(약 26MB)만 별도 폴더입니다.

## 2. 지금까지 완료된 것
- 전체 기능 구현 완료: 홈, SOS 표지판 모드, 가이드 모드, 진정 5종(숨쉬기/찬기운/사실되받기/파도흘려보내기/그라운딩), 음악 재생(증상 매칭 3스타일×2곡), 승무원 카드(기내용), 공황이란?, 이렇게 만들어졌습니다(근거 페이지), 언어토글(한/영), 개인정보 안전문구
- 실사용 테스트 진행 중이며 다음 버그/개선을 이미 반영함:
  1. **SOS 버튼 미작동 버그 수정 완료**: JS 문자열 이스케이프 오류(`\\'`가 여러 곳에 잘못 들어가 전체 `<script>` 파싱 실패) 전수 수정, `node --check`로 문법 검증 통과
  2. SOS 표지판 모드의 각 카드 문구를 "상태 설명"에서 "행동 지시"로 전면 수정: wave(파도) 카드 "지나간다"→"그냥 보기", fact 카드 "괜찮다"→"괜찮다 — 되뇌기", cold 카드 "찬물"→"찬물 — 얼굴에", ground 카드 "주변 보기"→"보이는 것 5개 찾기"
  3. 일반 파도 흘려보내기 화면도 동일하게 지시형으로 수정 + 텍스트를 크고 진하게(공 애니메이션 옆, 눈에 띄는 위치)
  4. 승무원 카드 진입 버튼에 "나를 도와주세요/Help me" 라벨 추가(원래 이모지 아이콘만 있어서 의미 파악 불가 문제 해결)
  5. 승무원 카드 뒷면에 복용약·알레르기 **실제 입력/저장 기능 추가** — localStorage에 저장(서버 전송 없음), 이전엔 입력 UI 자체가 없었음
  6. 홈 화면의 "공황이란?"·"이렇게 만들어졌습니다" 카드 색상을 다른 진정기능 카드(세이지그린)와 구분되는 베이지톤(`--ev-card:#ecdfc8`)으로 변경

## 3. 미해결/다음 할 일 (우선순위 순)
### 3-1. 최우선 — 오프라인(PWA) 동작 실제 검증
지금까지 테스트는 전부 `file://index.html`을 브라우저로 직접 열어서 진행했습니다. **이 방식은 서비스워커가 등록되지 않아 오프라인 캐싱이 검증되지 않은 상태입니다.** 안티그래비티에서 폴더를 마운트하면, 로컬 서버(`python3 -m http.server` 등)로 `http://localhost:PORT`로 접속해서:
1. 서비스워커(sw.js)가 정상 등록되는지 (Chrome 개발자도구 → Application → Service Workers)
2. 음악 6곡을 한 번씩 재생해 캐시 확인
3. Network 탭에서 Offline 체크 후 새로고침 → 앱이 그대로 뜨는지, SOS/가이드모드/음악 재생이 오프라인에서 되는지
이 검증을 반드시 해야 합니다.

### 3-2. 안드로이드 플레이스토어 배포 준비 (아직 시작 안 함)
`공황앱_음악근거_메모.md`의 "G. 안드로이드 배포 준비사항" 섹션에 상세 정리되어 있음. 요약하면:
- 실제 도메인 호스팅 필요 (TWA는 Digital Asset Links 검증이 있어야 함 — `/.well-known/assetlinks.json`)
- Bubblewrap으로 PWA → TWA 패키징
- 구글 개발자 계정($25), 비공개 테스트 14일+테스터 12명 필수(신규 계정 정책)
- **의료/건강 앱 정책 주의**: "치료/완치" 표현 금지, "자가관리 보조" 톤 유지(이미 반영됨)
- Data Safety 섹션에 "수집 데이터 없음" 정확히 신고 (실제 코드와 일치해야 함 — 현재 설계상 일치함)
- 개인정보처리방침 페이지 별도 필요
- 앱 아이콘(512×512), 피처그래픽(1024×500), 스크린샷 등 스토어 에셋 준비 안 됨

### 3-3. 아직 검토 안 된 항목
- "다른 방법 보기" 버튼(각 진정 화면 하단) → 누르면 무조건 홈으로 돌아가는데, 이 동작이 사용자 기대와 맞는지 재검토 필요하다고 짚었었으나 "일단 다음으로 진행"하기로 하고 보류된 상태
- iOS/사파리 오프라인 저장 방식은 별도 확인 안 함

## 4. 반드시 지켜야 할 설계 원칙 (수정/추가 작업 시 절대 어기면 안 되는 것들)
1. **모든 대처법은 임상 근거 기반이어야 함.** 근거 없이 그냥 "느낌상 좋을 것 같아서" 기능을 추가하면 안 됨. 각 기능의 근거는 `공황앱_음악근거_메모.md`와 index.html의 evidence 페이지(`#evidence`)에 매핑되어 있음.
2. **발작 중 화면(SOS/가이드모드)은 "표지판" 원칙**: 큰 그림 + 핵심 단어 1개(2~4글자, 34px 이상)만. 설명문 추가 금지. 이번 세션에서 배운 교훈 — 단어 하나만 있을 때 그게 "상태 설명"인지 "행동 지시"인지 반드시 검토할 것. 애매하면 사용자가 바로 헷갈려함.
3. **개인정보는 기기 밖으로 절대 안 나감.** 서버 전송 코드를 추가하면 안 됨. 저장이 필요하면 반드시 localStorage만 사용.
4. **학자 이름 언급 시 "감수/추천"처럼 과장 금지.** 반드시 "이 이론을 근거로 삼았다"로 정확히 표기(허위광고 방지, 구글 플레이스토어 의료앱 정책과도 연결).
5. **"치료/완치" 같은 의료 행위를 암시하는 단어 금지.** "자가관리 보조 도구"로 일관되게 표현.
6. **웹/플레이스토어 코드는 통일된 단일 PWA 코드베이스**로 유지. 별도 분기 코드 만들지 말 것.
7. **디자인 확정 사항**: 폰트 Maruburi(마루부리), 아이콘은 굵은 선 아이콘(이모지 금지, 승무원 카드 예외), 색상은 세이지그린(평상시 밝음 #e9efe6 / 발작중 어두움 #1c2620), 승무원 카드만 고대비 예외 팔레트(#f7f5ef 배경+검정 글씨).

## 5. 코드 수정 시 체크리스트
index.html은 HTML 안에 인라인 `<script>` 하나로 전체 JS가 들어있는 구조입니다. 수정 후에는 반드시 문법 검증을 거칠 것 (이번 세션에서 문자열 이스케이프 오류로 전체 기능이 멎었던 사고가 있었음):
```bash
node -e "
const fs = require('fs');
const html = fs.readFileSync('index.html', 'utf8');
const m = html.match(/<script>([\s\S]*?)<\/script>/);
fs.writeFileSync('/tmp/panic_check.js', m[1]);
"
node --check /tmp/panic_check.js && echo "SYNTAX OK"
```

## 6. 참고 문서
- `/Users/soo/AI_agents_20260516/panic/공황앱_음악근거_메모.md` — 전체 설계결정·근거·확정사항의 원본 기록. 새로운 기능을 추가하거나 애매한 판단이 필요할 때 반드시 먼저 이 파일을 참고할 것.
